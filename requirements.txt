# FastAPI 및 관련 패키지
fastapi==0.109.0  # 최신 버전으로 업그레이드
uvicorn[standard]==0.27.0  # 최신 버전으로 업그레이드
gunicorn==21.2.0

# Pydantic v2 (Python 3.13 호환)
pydantic==2.5.3
pydantic-settings==2.1.0
pydantic-core==2.14.6

# AI/ML 관련
google-generativeai==0.3.2  # 최신 버전으로 업그레이드

# YouTube 관련
youtube-transcript-api==0.6.2  # 최신 버전으로 업그레이드

# 웹소켓
websockets==12.0

# 환경 설정
python-dotenv==1.0.0

# HTTP 클라이언트
httpx==0.26.0  # 최신 버전으로 업그레이드
aiofiles==23.2.1
python-multipart==0.0.6

# 데이터베이스 (선택사항)
redis==5.0.1
sqlalchemy==2.0.25  # 최신 버전으로 업그레이드
alembic==1.13.1  # 최신 버전으로 업그레이드

# 인증/보안
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
cryptography==41.0.7

# 날짜/시간
python-dateutil==2.8.2
pytz==2023.3

# 유효성 검사
email-validator==2.1.0.post1  # 버그 수정 버전

# 로깅/모니터링
python-json-logger==2.0.7
prometheus-fastapi-instrumentator==6.1.0
sentry-sdk[fastapi]==1.39.1

# 테스트 도구 (개발용)
pytest==7.4.4  # 최신 버전으로 업그레이드
pytest-asyncio==0.23.3  # Python 3.13 호환 버전
pytest-cov==4.1.0
pytest-mock==3.12.0

# API 테스트
respx==0.20.2

# 타입 체킹 (Python 3.13 호환)
typing-extensions==4.9.0
