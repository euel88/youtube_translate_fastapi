# FastAPI 핵심 패키지
fastapi==0.104.1
uvicorn[standard]==0.24.0
gunicorn==21.2.0

# Google Gemini API
google-generativeai==0.3.0

# YouTube 자막 추출
youtube-transcript-api==0.6.1

# WebSocket 지원
websockets==12.0

# 데이터 검증 및 설정
pydantic==1.10.13
# pydantic-settings==2.1.0  # 제거됨 (pydantic 1.x와 충돌)
python-dotenv==1.0.0

# HTTP 클라이언트 및 파일 처리
httpx==0.25.2
aiofiles==23.2.1
python-multipart==0.0.6

# 데이터베이스 및 캐싱 (선택사항)
redis==5.0.1
sqlalchemy==2.0.23
alembic==1.13.0
# asyncpg==0.29.0  # ❌ 제거! Python 3.13과 호환 안 됨

# 보안 패키지
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
cryptography==41.0.7

# 유틸리티
python-dateutil==2.8.2
pytz==2023.3
email-validator==2.1.0

# 로깅 및 모니터링
python-json-logger==2.0.7
prometheus-fastapi-instrumentator==6.1.0
sentry-sdk[fastapi]==1.39.1

# 테스트 패키지 (개발용)
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
pytest-mock==3.12.0

# httpx 모킹 라이브러리
respx==0.20.2
